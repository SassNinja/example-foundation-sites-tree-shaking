# example-foundation-sites-tree-shaking

Example showing how to use webpack's tree-shaking wiht Foundation for Sites.

To run the webpack build simply execute (after `npm install`)

```bash
npm start
```